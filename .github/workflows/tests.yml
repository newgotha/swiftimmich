name: Tests

on:
  # By hand for now; add push / pull_request triggers once a macOS 26 runner is confirmed.
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - run: swift test
